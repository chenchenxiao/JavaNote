JVM就是模拟一个计算机来达到一个计算机所具有的计算功能

体系结构：
    1、类加载器
        在JVM启动时或类运行时将需要的class加载到JVM中
    2、执行引擎
        负责执行class文件中包含的字节码指令，相当于CPU
        解析JVM字节码指令，得到执行结果
    3、内存区
        将内存划分为若干个区以模拟实际机器上的存储等模块
    4、本地方法调用
        调用本地方法，返回结果